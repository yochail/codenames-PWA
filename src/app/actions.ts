 //import { GameCardState } from "./store";
// import { createAction } from "@reduxjs/toolkit";

 export const FLIP_CARD = "FLIP_CARD"
// const flipCardAction = createAction<number>(FLIP_CARD)

// export const INIT_BOARD = "INIT_BOARD"
// const initBoardAction = createAction<GameCardState[]>(INIT_BOARD)

// export type BoardAction = ReturnType<typeof flipCardAction> | ReturnType<typeof initBoardAction>
